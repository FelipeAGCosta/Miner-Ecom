# =========================================================
# OS / Sistema
# =========================================================
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# =========================================================
# Python (bytecode / caches)
# =========================================================
__pycache__/
*.py[cod]
*$py.class
*.so

# =========================================================
# Python - venvs / ambientes
# =========================================================
.venv/
env/
venv/
ENV/
env.bak/
venv.bak/

# =========================================================
# Testes / cobertura / tooling
# =========================================================
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
.mypy_cache/
.dmypy.json
ruff_cache/
.pyre/
.pytype/
.hypothesis/
.ipynb_checkpoints/
profile_default/
ipython_config.py

# =========================================================
# Build / distribuição Python
# (ajustado para NÃO ignorar src/lib do projeto)
# =========================================================
.Python
build/
dist/
downloads/
eggs/
.eggs/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
*.manifest
*.spec

# =========================================================
# Logs / arquivos temporários (geral)
# =========================================================
*.log
*.tmp
*.temp

# Saídas de dados (se quiser versionar alguma, remova aqui)
*.csv

# Backups
*.bak
*.bak_*
search_tasks.yaml.bak_*

# =========================================================
# IDEs / editores
# =========================================================
.vscode/
.idea/
*.iml

# Cursor
.cursorignore
.cursorindexingignore

# =========================================================
# Segredos / env files (NUNCA versionar)
# =========================================================
.env
.env.*
!.env.example
!.env.template

# Next.js / Node também usa isso:
.env.local
.env.development.local
.env.test.local
.env.production.local

# =========================================================
# Node / Next.js (Front)
# =========================================================
node_modules/
.next/
out/
.turbo/
.vercel/

# Logs do Node
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# =========================================================
# Projeto Miner Ecom — arquivos privados/locais
# =========================================================

# Configuração real de categorias/tarefas (mantida somente local)
search_tasks.yaml
search_tasks.backup.yaml
config/search_tasks.yaml
assets/search_tasks.yaml
search_tasks_ebay.yaml

# Estados do crawler
crawler_state.json
crawler_state_ebay.json
crawler_state_ebay_seeded.json
crawler_state_*.json

# Scripts de agendamento / automações locais
*.bat

# Logs locais
logs/

# Scripts utilitários locais (não subir pro repo)
sync_browse_nodes_to_yaml.py
update_browse_node_candidates.py

# Heurísticas privadas (não versionar)
lib/bsr_heuristics_private.py

# Dependências específicas deste ambiente (se não quiser versionar)
requirements.txt

# Integrações privadas (SP-API, credenciais, etc.)
integrations/

# Crawlers internos (não subir pro GitHub)
crawlers_ebay/
backfill_amazon_images_from_matchmap.py
compute_match_map_image_distance.py

# Se estes arquivos na raiz forem locais/privados, mantenha ignorados:
crawler_amazon.py
crawler_amazon_batch.py
run_crawler_daily.bat