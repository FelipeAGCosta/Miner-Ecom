[theme]
# Cor principal (botões, elementos ativos)
primaryColor = "#6366F1"

# Fundo global do app (dark mais claro)
backgroundColor = "#0B1224"

# Fundo padrão de containers internos (cards/elementos)
secondaryBackgroundColor = "#111827"

# Cor padrão do texto
textColor = "#E5E7EB"

# Fonte
font = "sans"
